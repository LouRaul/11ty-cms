---
layout: layouts/base.njk
title: Contact Us
description: Get in touch with our team for any questions about our products, orders, or sustainability practices.
---

<div class="container mx-auto px-4 py-12">
    <!-- Breadcrumb -->
    <nav class="mb-8" aria-label="Breadcrumb">
        <ol class="flex text-sm">
            <li><a href="/" class="text-blue-600 hover:text-blue-800">Home</a></li>
            <li class="mx-2">/</li>
            <li aria-current="page">Contact</li>
        </ol>
    </nav>

    <!-- Page Header -->
    <header class="text-center mb-12">
        <h1 class="text-4xl font-bold mb-4">Contact Us</h1>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
            Have questions about our products or sustainability practices? We're here to help! Choose your preferred way to reach us below.
        </p>
    </header>

    <div class="grid md:grid-cols-2 gap-12 mb-12">
        <!-- Contact Form -->
        <div class="bg-white p-6 rounded-lg shadow-lg"
             x-data="{
                name: '',
                email: '',
                subject: '',
                message: '',
                submitted: false,
                loading: false,
                success: false,
                error: false,
                submitForm() {
                    if (this.loading) return;
                    this.loading = true;
                    this.error = false;
                    
                    // Simulate form submission
                    setTimeout(() => {
                        this.loading = false;
                        this.success = true;
                        this.submitted = true;
                    }, 1000);
                }
             }">
            <h2 class="text-2xl font-bold mb-6">Send Us a Message</h2>
            
            <!-- Success Message -->
            <div x-show="success" 
                 class="bg-green-50 text-green-800 p-4 rounded-lg mb-6"
                 role="alert">
                <p class="font-bold">Message Sent!</p>
                <p>Thank you for contacting us. We'll get back to you within 24 hours.</p>
            </div>

            <!-- Error Message -->
            <div x-show="error"
                 class="bg-red-50 text-red-800 p-4 rounded-lg mb-6"
                 role="alert">
                <p class="font-bold">Oops!</p>
                <p>Something went wrong. Please try again or use another contact method.</p>
            </div>

            <!-- Contact Form -->
            <form x-show="!submitted"
                  @submit.prevent="submitForm"
                  class="space-y-4">
                <div>
                    <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Name</label>
                    <input type="text"
                           id="name"
                           name="name"
                           x-model="name"
                           required
                           class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500"
                           autocomplete="name">
                </div>

                <div>
                    <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                    <input type="email"
                           id="email"
                           name="email"
                           x-model="email"
                           required
                           class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500"
                           autocomplete="email">
                </div>

                <div>
                    <label for="subject" class="block text-sm font-medium text-gray-700 mb-1">Subject</label>
                    <select id="subject"
                            name="subject"
                            x-model="subject"
                            required
                            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                        <option value="">Select a subject</option>
                        <option value="product">Product Inquiry</option>
                        <option value="order">Order Status</option>
                        <option value="returns">Returns & Exchanges</option>
                        <option value="sustainability">Sustainability Questions</option>
                        <option value="other">Other</option>
                    </select>
                </div>

                <div>
                    <label for="message" class="block text-sm font-medium text-gray-700 mb-1">Message</label>
                    <textarea id="message"
                             name="message"
                             x-model="message"
                             required
                             rows="4"
                             class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500"></textarea>
                </div>

                <button type="submit"
                        class="w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg transition duration-300 disabled:opacity-50"
                        :disabled="loading">
                    <span x-show="!loading">Send Message</span>
                    <span x-show="loading">Sending...</span>
                </button>
            </form>
        </div>

        <!-- Contact Information -->
        <div class="space-y-8">
            <!-- Quick Contact -->
            <div class="bg-white p-6 rounded-lg shadow-lg">
                <h2 class="text-2xl font-bold mb-6">Quick Contact</h2>
                <div class="space-y-4">
                    <div class="flex items-start space-x-4">
                        <div class="text-green-600">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                            </svg>
                        </div>
                        <div>
                            <h3 class="font-bold">Email Us</h3>
                            <a href="mailto:{{ contact.email }}" class="text-blue-600 hover:text-blue-800">
                                {{ contact.email }}
                            </a>
                            <p class="text-sm text-gray-600 mt-1">Response within 24 hours</p>
                        </div>
                    </div>

                    <div class="flex items-start space-x-4">
                        <div class="text-green-600">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                            </svg>
                        </div>
                        <div>
                            <h3 class="font-bold">Call Us</h3>
                            <a href="tel:{{ contact.phone }}" class="text-blue-600 hover:text-blue-800">
                                {{ contact.phone }}
                            </a>
                            <p class="text-sm text-gray-600 mt-1">Mon-Fri, 9am-5pm EST</p>
                        </div>
                    </div>

                    <div class="flex items-start space-x-4">
                        <div class="text-green-600">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                            </svg>
                        </div>
                        <div>
                            <h3 class="font-bold">Visit Us</h3>
                            <address class="not-italic">
                                {{ contact.address | nl2br | safe }}
                            </address>
                        </div>
                    </div>
                </div>
            </div>

            <!-- FAQ Section -->
            <div class="bg-white p-6 rounded-lg shadow-lg">
                <h2 class="text-2xl font-bold mb-6">Frequently Asked Questions</h2>
                <div class="space-y-4">
                    <div>
                        <h3 class="font-bold">What's your return policy?</h3>
                        <p class="text-gray-600">We offer a 30-day return policy for unused items in original packaging.</p>
                    </div>
                    <div>
                        <h3 class="font-bold">How long does shipping take?</h3>
                        <p class="text-gray-600">Standard shipping takes 3-5 business days within the continental US.</p>
                    </div>
                    <div>
                        <h3 class="font-bold">Do you ship internationally?</h3>
                        <p class="text-gray-600">Yes, we ship to select countries. See our <a href="/shipping" class="text-blue-600 hover:text-blue-800">shipping page</a> for details.</p>
                    </div>
                </div>
                <a href="/faqs" class="text-green-600 hover:text-green-700 font-bold mt-4 inline-block">
                    View all FAQs →
                </a>
            </div>

            <!-- Social Media -->
            <div class="bg-white p-6 rounded-lg shadow-lg">
                <h2 class="text-2xl font-bold mb-6">Connect With Us</h2>
                <div class="flex space-x-4">
                    {% for social in site.social %}
                    <a href="{{ social.url }}"
                       class="text-gray-600 hover:text-green-600 transition duration-300"
                       aria-label="Follow us on {{ social.title }}">
                        <svg class="w-6 h-6" fill="currentColor" role="img" aria-hidden="true">
                            <use href="/assets/icons/social-icons.svg#{{ social.icon }}"/>
                        </svg>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>

    <!-- Map Section -->
    <div class="bg-white p-6 rounded-lg shadow-lg">
        <h2 class="text-2xl font-bold mb-6">Our Location</h2>
        <div class="aspect-w-16 aspect-h-9">
            <!-- Replace with actual map implementation -->
            <div class="bg-gray-100 w-full h-full flex items-center justify-center">
                <p class="text-gray-500">Map placeholder - Implement with your preferred mapping service</p>
            </div>
        </div>
    </div>
</div>